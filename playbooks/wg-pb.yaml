
#<-- Configure VPN for onprem & VPS & Cloud (wirguard) -->#
- hosts: wg-server
  vars:
    - reset_vpn: false     # True = 1st time & resets
    - vpn_cidr: 10.7.7.1/24
    - wg_port: 5755 # default is 51820
  roles:
    - wireguard-server

#<-- Add VPN clients (wirguard) -->#
- hosts: wg-client
  vars:
    - reset_vpn: false     #True not required for 1st time
    - wg_server_ip: 10.0.0.115
    - vpn_cidr: 10.7.7.1/24
    - wgc_cidr: 10.7.7
    - wg_port: 5755 
  roles:
    - wireguard-client
